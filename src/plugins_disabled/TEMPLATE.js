import GUI from 'utils/gui';
import Koc from 'utils/koc_utils';

var db = Koc.db;

export default {
    description: "",

    defaultEnabled: true,

    enabledPages: ['armory'],

    run: function() {
    }
};
