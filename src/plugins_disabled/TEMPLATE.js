define([
    'utils/gui',
    'utils/koc_utils'
], function(GUI, Koc) {

    var db = Koc.db;

    return {
        description: "",

        defaultEnabled: true,

        enabledPages: ['armory'],

        run: function() {
        }
    }
});
